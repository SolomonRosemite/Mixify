query Configurations($id: ID!) {
  configurations(id: $id) {
    id
    playlists {
      id
      name
      spotifyPlaylistId
      isMixstack
      playlistOrder
      associations {
        id
        childPlaylistId
        parentPlaylistId
      }
    }
  }
}
